# Template metadata for a new agent.
# Replace placeholder values after scaffolding your own agent project.
namespace: agentsystems            # Publisher / organization namespace
name: your_agent_name              # Unique name within the namespace
version: 0.1.0                     # Semantic version

description: "Short description of what the agent does."

# List of LLM or other model names required for this agent to run.
# These should match the model IDs you use in get_model() calls in main.py
model_dependencies:
  - claude-sonnet-4
  # - gpt-5-nano
  # - llama3.3:70b
  # - gemma3:4b
